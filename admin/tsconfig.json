{
  "extends": "@strapi/typescript-utils/tsconfigs/admin",

  "compilerOptions": {
    "rootDir": "../",
    "baseUrl": ".",
    "paths": {
      "#config": ["../common/config"]
    }
  },

  "include": [
    "../common/**/*",
    "./custom.d.ts",

    "**/*",
    "**/*.json"
  ],

  "exclude": ["**/*.test.ts"]
}
