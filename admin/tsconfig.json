{
  "extends": "@strapi/typescript-utils/tsconfigs/admin",

  "compilerOptions": {
    "rootDir": "../",
    "baseUrl": ".",
    "noEmit": true,
    "paths": {
      "#config": ["../common/config"]
    }
  },

  "include": [
    "../common/**/*",
    "./custom.d.ts",

    "**/*",
    "**/*.json"
  ]
}
